source 'https://rubygems.org'

# Same gem set GitHub Pages builds the site with; the lockfile is resolved on
# the Ruby version recorded in .ruby-version.
gem 'github-pages'
gem 'jekyll-redirect-from'

group :test do
  gem 'minitest'
  gem 'bundler-audit'
end
